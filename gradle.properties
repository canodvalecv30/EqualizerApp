# AndroidX
android.useAndroidX=true
android.enableJetifier=true

# JVM
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Kotlin (por si acaso)
kotlin.code.style=official

# Non-transitive R class (más rápido)
android.nonTransitiveRClass=true
